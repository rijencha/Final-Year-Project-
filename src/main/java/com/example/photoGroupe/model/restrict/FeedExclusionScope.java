package com.example.photoGroupe.model.restrict;

public enum FeedExclusionScope {
    PIN,       // hide one specific pin
    USER,      // hide all pins from one user
    CATEGORY   // hide all pins under one category
}