package com.example.photoGroupe.model.restrict;

public enum RestrictionType {
    CHAT,       // restricted user cannot start/send messages to the restrictor
    COMMENT,    // restricted user cannot comment on the restrictor's pins
    TAG
}
